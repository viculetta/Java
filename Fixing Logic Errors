public class Main{
  public static void main(String[] args) {
    int steps = 10;
    
    for (int i = 1; i <= steps; i++) { // if we would start with i = 0, then we would have steps from 0 to 10 instead of 1 to 10.
      System.out.println("Step #" + i);
    }
  }
}
